# Specs

One file per feature, written before the feature is built.

Run `/spec <feature description>` to generate one.

A spec describes behaviour, not implementation. If you can't tell from reading
it whether the finished feature is correct, the spec isn't done.
